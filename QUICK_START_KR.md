# 빠른 시작 가이드 - 파란색 큐브 만들기

## 개요

BlenderMCP를 사용하여 블렌더에 파란색 큐브를 만드는 가장 간단한 방법들을 안내합니다.

## 준비사항 확인

시작하기 전에 다음을 확인하세요:

- [ ] Blender 5.0.1 이상 설치
- [ ] Python 3.8 이상 설치
- [ ] BlenderMCP 저장소 클론 완료

## 방법 1: Blender에서 직접 실행 ⚡ (초보자 권장)

**MCP 설정 없이도 사용 가능합니다!**

### 단계별 가이드

1. **Blender 실행**
   ```
   Blender 5.0.1을 실행하세요
   ```

2. **Scripting 작업공간으로 전환**
   - 상단 메뉴에서 `Scripting` 클릭
   - 또는 `+` 버튼으로 새 작업공간 생성

3. **스크립트 열기**
   - `Text > Open` 메뉴 선택
   - `blender_create_blue_cube.py` 파일 선택

4. **실행**
   - `Run Script` 버튼 클릭 (또는 `Alt + P`)

5. **결과 확인**
   - 3D 뷰포트에 파란색 큐브가 나타납니다
   - 쉐이딩 모드가 자동으로 Material Preview로 변경됩니다

### 스크립트 위치

```
BlenderMCP/blender_create_blue_cube.py
```

### 예상 출력

```
============================================================
파란색 큐브 생성 시작
============================================================
✓ 큐브 생성됨: BlueCube
✓ 파란색 재질 설정됨
✓ 파란색 큐브 생성 완료!
============================================================
완료! 큐브 이름: BlueCube
위치: <Vector (0.0000, 0.0000, 0.0000)>
크기: <Vector (1.0000, 1.0000, 1.0000)>
============================================================
```

---

## 방법 2: MCP 서버를 통한 실행 🚀 (개발자 권장)

**Blender와 실시간으로 통신합니다!**

### 사전 준비

1. **Blender MCP Addon 설치**
   - Blender에서 `Edit > Preferences > Add-ons`
   - `Install` 버튼 클릭
   - `addon.py` 파일 선택
   - "Blender MCP Server" 검색 후 활성화

2. **서버 시작 확인**
   - Blender 우측 사이드바에 `MCP` 탭 확인
   - "Server Status: Running" 표시 확인
   - Port: 9876

### 실행 방법

터미널(또는 명령 프롬프트)에서:

```bash
cd BlenderMCP
python3 create_blue_cube.py
```

### 예상 출력

```
============================================================
Creating Blue Cube in Blender
============================================================

[1/2] Creating cube...
✓ Connected to Blender at localhost:9876
✓ Cube created: BlueCube

[2/2] Setting blue color...
✓ Blue material applied: BlueCube

============================================================
✓ Blue cube created successfully!
============================================================

[Optional] Capturing viewport screenshot...
✓ Screenshot captured (base64 data available)
```

---

## 방법 3: Claude Desktop 통합 💬 (최고급)

**자연어로 Blender를 제어합니다!**

### 설정

1. **Claude Desktop 설정 파일 편집**

   Windows:
   ```
   %APPDATA%\Claude\claude_desktop_config.json
   ```

   macOS/Linux:
   ```
   ~/Library/Application Support/Claude/claude_desktop_config.json
   ```

2. **설정 내용 추가**
   ```json
   {
     "mcpServers": {
       "blender": {
         "command": "python",
         "args": [
           "/절대/경로/BlenderMCP/blender_mcp_server.py"
         ],
         "env": {
           "BLENDER_HOST": "localhost",
           "BLENDER_PORT": "9876"
         }
       }
     }
   }
   ```

3. **Claude Desktop 재시작**

### 사용 예시

Claude Desktop에서 다음과 같이 입력하세요:

```
블렌더에 파란색 큐브를 만들어줘
```

또는 더 구체적으로:

```
블렌더에 (0, 0, 0) 위치에 크기 2인 파란색 큐브를 만들고,
이름을 "MyCube"로 설정해줘
```

Claude가 자동으로:
1. Blender에 연결
2. 큐브 생성
3. 파란색 재질 적용
4. 결과 확인

---

## 문제 해결

### "Connection refused" 에러

**원인**: Blender가 실행되지 않았거나 MCP addon이 비활성화됨

**해결 방법**:
1. Blender 실행 확인
2. MCP addon 활성화 확인
3. MCP 탭에서 "Start Server" 버튼 클릭
4. 포트 9876이 다른 프로그램에서 사용 중인지 확인

### 큐브가 보이지 않음

**해결 방법**:
1. 쉐이딩 모드를 `Material Preview` 또는 `Rendered`로 변경
2. 뷰포트 초기화: 마우스를 3D 뷰 위에 두고 `Home` 키 또는 `View > Frame All`
3. 아웃라이너(Outliner)에서 "BlueCube" 객체 확인

### 색상이 제대로 보이지 않음

**해결 방법**:
1. 뷰포트 쉐이딩을 `Solid`에서 `Material Preview`로 변경
2. 조명 추가: `Add > Light > Sun`
3. 렌더 엔진 확인: `Render Properties > Render Engine > Cycles` 또는 `Eevee`

---

## 다음 단계

파란색 큐브 생성에 성공했다면, 다음을 시도해보세요:

1. **다른 색상 큐브 만들기**
   ```python
   # blender_create_blue_cube.py를 복사하여 색상 값 변경
   principled.inputs["Base Color"].default_value = (1.0, 0.0, 0.0, 1.0)  # 빨간색
   ```

2. **다른 형태 만들기**
   - 구(Sphere): `bpy.ops.mesh.primitive_uv_sphere_add()`
   - 원기둥(Cylinder): `bpy.ops.mesh.primitive_cylinder_add()`
   - 원뿔(Cone): `bpy.ops.mesh.primitive_cone_add()`

3. **여러 객체 한번에 만들기**
   - `EXAMPLES.md` 파일 참조

---

## 추가 자료

- 📖 [전체 문서](README.md)
- 💡 [사용 예제](EXAMPLES.md)
- 🔧 [Blender Python API](https://docs.blender.org/api/current/)

---

## 요약

| 방법 | 난이도 | 속도 | MCP 필요 | 추천 대상 |
|------|--------|------|----------|-----------|
| 방법 1: Blender 직접 | ⭐ | 느림 | ❌ | Blender 초보자 |
| 방법 2: Python 스크립트 | ⭐⭐ | 빠름 | ✅ | 개발자 |
| 방법 3: Claude Desktop | ⭐⭐⭐ | 매우 빠름 | ✅ | 고급 사용자 |

**처음 사용하신다면 방법 1부터 시작하세요!** ✨
